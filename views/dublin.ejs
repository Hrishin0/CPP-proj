<%- include partials/header.ejs %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> <% title %></title>
  <style>
    body {
      font-family: sans-serif;
    }

    .heading {
      text-align: center;
      padding: 2rem;
    }
    .image-container {
        background-image: url("<%= randomImageURL %>");
        background-size: cover;
        position: relative;
        height: 92vh;

    }

    .factText {
        background-color: white;
        color: black;
        font-size: 5vw; 
        font-weight: bold;
        margin: 0 auto;
        padding: 10px;
        width: 50%;
        text-align: center;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        mix-blend-mode: screen;
    }
    button{
        margin-top: 40vh;
        margin-left: 10vw;
    }
  </style>
</head>
<body onload="dublinFacts(), changeImage()">
    <div class="image-container">
        <h1 class="factText" id="hh"></h1>
        <button class="btn btn-light" onclick="change()"> Learn a random fact about Dublin</button>
    </div>
    <script src="../public/javascript/script.js"></script>
   <script>
    let city;
    let x = Math.floor((Math.random() * 5) + 1);
    function dublinFacts(){
            fetch("https://0hh5sbohxd.execute-api.us-east-1.amazonaws.com/dev").then((response) => response.json()).then((data) => {
                facts = data.body.facts;
        });
    function changeImage(){
      const imageContainer = document.querySelector(".image-container");
      imageContainer.style.backgroundImage = `url(${dublinImage})`;
    };
    }
    function change(){
        const randomIndex = Math.floor((Math.random() * facts.length) + 1);
        city = facts[randomIndex - 1]; 
        document.getElementById("hh").innerHTML = city;
    };
  </script>
  </body>
</html>


